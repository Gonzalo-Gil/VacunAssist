<div class="container" id="nuevo_turno">
    <h2 style="margin-top: 20px">Pedir un turno para: <%= @enfermedad.nombre %></h2>
    <%= form_for(@turno) do |f| %>
        <div class="form-group" style="margin-bottom: 10px">
            <div class="col-md-4">
                <%= f.label :sede_id, 'Elija la sede donde desea aplicarse la vacuna' %>
                <%= f.collection_select(:sede_id, Sede.all, :id, :nombre, {multiple: :true}, { class: 'form-control'}) %>
            </div>
        </div>
        <div class="form-group" style="margin-bottom: 10px">
            <div class="col-md-4">
                <%= f.label :fecha, 'Elija la fecha' %>
                <%= f.date_field :fecha, class: 'form-control', min: Date.tomorrow, required: true %>
            </div>
        </div>
        <div class="form-group" style="margin-bottom: 10px">
            <div class="col-md-4">
                <%= f.hidden_field 'enfermedad_id', value: @enfermedad.id %>
            </div>
        </div>
        
        <div class="actions" style="margin-bottom: 10px">
            <%= f.submit "Guardar", class: 'btn btn-primary' %>
            <%= link_to "Cancelar", turnos_pedir_url, class: 'btn btn-danger' %>
        </div>
    <% end %>
</div>