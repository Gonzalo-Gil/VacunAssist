<div class="container">
  <h2>Editar perfil</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :nombre, 'Nombre/s' %>
          <%= f.text_field :nombre, class: 'form-control', required: true, placeholder: 'Ingrese el nombre' %>
        </div>
      </div>

      <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :apellido, 'Apellido/s' %>
          <%= f.text_field :apellido, class: 'form-control', required: true, placeholder: 'Ingrese el apellido' %>
        </div>
      </div>

      <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :telefono %><br />
          <%= f.telephone_field :telefono, class: 'form-control', required: true, placeholder: "Ingrese su número de telefono" %>
        </div>
      </div>

      <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :email %><br />
          <%= f.email_field :email, class: 'form-control', autocomplete: "email", placeholder: "mail@ejemplo.com" %>
        </div>
      </div>

      <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :password, "Contraseña nueva" %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> caracteres como minimo)</em>
          <% end %><br />
          <%= f.password_field :password, class: 'form-control', autocomplete: "new-password", placeholder: "******" %>
        </div>
        <p>Si no desea cambiar la contraseña deje este campo en blanco.</p>
      </div>

      <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :password_confirmation, "Repita la contraseña nueva" %><br />
          <%= f.password_field :password_confirmation, class: 'form-control', autocomplete: "new-password", placeholder: "******" %>
        </div>
        <p>Si no desea cambiar la contraseña deje este campo en blanco.</p>
      </div>

      <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :current_password, "Contraseña actual"%>
          <%= f.password_field :current_password, class: 'form-control', placeholder: "******"%>
          <p style="color: red">Este campo es obligatorio</p>
        </div>
      </div>
      <% if current_user.role == 0 %>
      <div class="form-group">
        <div class="input-field" style="text-align: left">
          <%= f.check_box :notificacion, class: 'my-checkbox' %>
          <%= f.label :notificacion, "Deseo recibir notificaciones de mis proximos turnos", class: 'form-check-label', style:"font-size: 20px" %>
        </div>
      </div>

      <div class="form-group" style="margin-bottom: 10px">
        <div class="col-md-4">
          <%= f.label :medio_notificacion, 'Medio para recibir notificaciones' %>
          <%= f.select :medio_notificacion, ['','Correo electrónico', 'Mensaje de texto'], {multiple: :true}, { class: 'form-control'} %>
        </div>
      </div>
    <% end %>
    <div class="form-actions">
      <%= f.submit "Guardar", class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>